# v0.dev 提示词 - 健身计划追踪应用

## 直接复制使用的提示词

```
创建一个现代、美观的每日健身计划追踪应用，使用 Vue 3 和 TypeScript。

**核心功能**：
1. 顶部显示当前日期（格式：YYYY年MM月DD日）
2. 添加新计划按钮（浮动圆形按钮，带 + 图标）
3. 健身计划列表（卡片式设计）
   - 每个卡片包含：健身项目名称、完成复选框、编辑图标按钮、删除图标按钮
   - 已完成的项目显示绿色勾选状态和删除线效果
   - 未完成的项目显示为正常状态
4. 底部显示统计信息：总计划数 / 已完成数 / 完成率
5. 添加/编辑计划模态框，包含输入框和保存/取消按钮

**设计风格**：
- 背景：渐变色（橙色 #ff6b6b → 粉色 #feca57）
- 卡片：白色背景，圆角阴影，现代化设计
- 颜色：完成状态使用绿色 #51cf66
- 文本：深灰色 #2d3436
- 强调色：蓝色 #0984e3
- 图标：使用 Heroicons 或 Phosphor Icons
- 字体：现代化无衬线字体
- 响应式布局，适配移动端和桌面端

**交互效果**：
- 卡片添加：淡入 + 上滑动画
- 卡片删除：滑出 + 淡出动画
- 复选框切换：缩放动画
- 完成任务：脉冲动画或绿色闪烁
- 点击模态框外部关闭
- 所有按钮有悬停和点击反馈

**技术细节**：
- 使用 Vue 3 Composition API
- 使用 TypeScript
- 使用 LocalStorage 存储数据（key: workout_data）
- 数据结构包含：id, name, completed, createdAt
- 数据每天自动清空（检测日期变化）
- 支持添加、编辑、删除、标记完成功能

**代码要求**：
- 组件化设计，易于维护
- 添加详细的注释
- 使用组合式 API 的最佳实践
- 添加类型定义
- 空状态处理（没有计划时显示友好提示）
```

## 详细分步提示词（可选）

如果你想分步骤构建，可以使用以下提示词：

### 步骤 1：创建基础结构

```
创建一个 Vue 3 项目的基础结构，包含：
- App.vue 主组件
- 顶部标题栏组件 WorkoutHeader.vue，显示当前日期
- 使用渐变色背景（橙色到粉色）
- 使用 TypeScript 和 Composition API
```

### 步骤 2：添加计划列表

```
创建健身计划列表组件 WorkoutList.vue，显示计划卡片。
每个卡片包含：
- 健身项目名称
- 完成复选框
- 编辑按钮
- 删除按钮
使用白色卡片设计，圆角阴影。
```

### 步骤 3：添加模态框

```
创建添加/编辑健身计划的模态框组件 WorkoutModal.vue。
包含：
- 输入框用于输入计划名称
- 保存和取消按钮
- 点击外部关闭
- 表单验证
```

### 步骤 4：添加数据存储

```
实现 LocalStorage 数据存储功能：
- 创建 useWorkouts composable
- 实现 CRUD 操作（增删改查）
- 添加每日自动清空逻辑
- 数据结构：id, name, completed, createdAt
```

### 步骤 5：添加统计信息

```
创建底部统计栏 StatisticsBar.vue，显示：
- 总计划数
- 已完成数
- 完成率（百分比）
- 使用现代设计风格
```

### 步骤 6：添加动画效果

```
为应用添加平滑的动画效果：
- 卡片添加动画
- 卡片删除动画
- 复选框切换动画
- 完成任务时的庆祝动画
使用 Vue Transition 和 CSS 动画
```

## 高级特性提示词（可选）

如果你想添加更多功能，可以使用：

### 添加健身目标

```
为每个健身计划添加目标功能：
- 目标次数（例如：50次俯卧撑）
- 已完成次数
- 进度条显示完成进度
- 完成后显示庆祝动画
```

### 添加历史记录

```
添加历史记录功能：
- 查看过去几天的健身记录
- 使用日历视图显示历史数据
- 统计每周/每月的完成情况
```

### 添加主题切换

```
添加深色模式支持：
- 主题切换按钮
- 深色配色方案
- 平滑的主题切换动画
- 使用 localStorage 保存主题偏好
```

## v0 使用技巧

### 1. 逐步构建
不要一次性提交所有需求，先提交核心功能，逐步添加特性。

### 2. 明确设计风格
在提示词中明确说明颜色、字体、布局等设计细节。

### 3. 指定技术栈
明确说明使用 Vue 3、TypeScript 等技术栈。

### 4. 描述交互效果
详细描述动画和交互效果，v0 会生成相应的代码。

### 5. 迭代优化
生成代码后，可以根据结果进一步优化提示词，再次生成。

## 常见问题

### Q: 如何在 v0 中指定样式？
A: 在提示词中详细描述颜色、字体、布局等设计细节。

### Q: 如何让 v0 生成 TypeScript 代码？
A: 在提示词中明确说明"使用 TypeScript"。

### Q: 如何添加动画效果？
A: 在提示词中描述具体的动画效果（如淡入、滑出等），v0 会使用 Vue Transition。

### Q: 如何确保代码质量？
A: 在提示词中添加"组件化设计"、"添加注释"等要求。

## 示例使用流程

1. 打开 [v0.dev](https://v0.dev)
2. 复制上面的"直接复制使用的提示词"
3. 粘贴到 v0 的输入框
4. 点击生成
5. 查看生成的代码
6. 根据需要微调提示词再次生成
7. 导出代码到本地项目

## 提示词优化建议

### 好的提示词特征：
- ✅ 明确的描述（"现代、美观"）
- ✅ 具体的设计细节（颜色代码、布局描述）
- ✅ 功能列表清晰
- ✅ 技术栈明确
- ✅ 交互效果详细

### 避免的提示词特征：
- ❌ 过于笼统（"做一个应用"）
- ❌ 缺乏设计细节
- ❌ 功能描述不清晰
- ❌ 未指定技术栈

## 后续开发步骤

生成代码后：
1. 将代码复制到项目中
2. 安装依赖：`npm install`
3. 添加需要的图标库（如 Heroicons）
4. 配置 Capacitor（参考 DEPLOYMENT.md）
5. 测试功能
6. 构建并部署

祝你开发顺利！💪
